## Usage

Documents can be generated using `doxygen`, it requires doxygen to be installed. To generate, run `doxygen Doxyfile`.

To build this project, you should have cmake installed.

Firstly, run `cmake -B build`.

Then, `cmake --build build`.

The binaries will compile into the build/ dir.